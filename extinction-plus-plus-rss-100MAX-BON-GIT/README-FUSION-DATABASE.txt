Base fusionnée avec :
- tes Discords, salons, rôles et serveurs depuis database(1).json
- les defaults V7, arkServers et dayzMaps depuis database(2).json
- logo/thème par défaut appliqué à chaque Discord
